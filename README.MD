# How to start

Dev (live reload on):  
Set NODE_ENV=development in server/.env and client/.env.development.  
Run: npm run dev in both /server and /client.  

Prod (no live reload):  
Set NODE_ENV=production in server/.env and client/.env.production.  
Run: npm start in /server; npm run build && npm run preview in /client.  

